package com.tomdog.reward.util;

public class LogUtil {

    //    static {
//        String logPath = StrUtil.emptyToDefault(System.getProperty("logPath"), "logback.xml");
//        ConfigurationSource source = ConfigurationSource.fromResource(logPath, ApplicationStarter.class.getClassLoader());
//        Configurator.initialize(null, source);
//    }

}
